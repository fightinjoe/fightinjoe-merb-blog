%h2= @blog.title
%small= @blog.created_at
%h3= # flash[:notice]
= @blog.body_html

= link( 'New',  url(:new_blog) )
= link( 'Edit', url(:edit_blog, @blog) )

= partial( 'comments/index', :blog => @blog )